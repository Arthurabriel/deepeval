name: Run DeepEval Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  experiment:
    name: Run Prompt Experiment
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Optimize prompt (generates optimized_prompt.json)
        working-directory: src
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python optimize_prompt.py

      - name: Run experiment
        working-directory: src
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python test_run_experiment.py

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: experiment-results
          path: src/results.json

  security:
    name: Run Security Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run security tests
        working-directory: src
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: python test_run_security.py

      - name: Upload security results
        uses: actions/upload-artifact@v4
        with:
          name: security-results
          path: src/security_results.json
